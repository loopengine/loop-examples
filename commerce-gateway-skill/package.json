{
  "name": "@loop-engine/commerce-gateway-example",
  "version": "0.1.0",
  "description": "Loop Engine × LLM Commerce Gateway integration example",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@loop-engine/core": "^0.1.0",
    "@loop-engine/runtime": "^0.1.0",
    "@loop-engine/sdk": "^0.1.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
